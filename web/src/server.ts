// Server entry for TanStack Start SSR. Kept minimal; Nitro wraps this into the
// deployable server build (node-server preset by default → Docker on ECS).
export { default } from "@tanstack/react-start/server-entry";
